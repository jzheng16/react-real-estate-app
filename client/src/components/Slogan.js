import React from 'react';

export default props => (
  <div className="slogan ">
    <div className="slogan-banner">
      No one in the world sells more real estate than BaibMax
    </div>
  </div>
);
